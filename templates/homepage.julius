var elem = document.querySelector('.grid');
var msnry0 = new Masonry( elem, {
  // options
  itemSelector: '.grid-item',
  columnWidth: 200
});
var msnry1 = new Masonry( document.querySelector('.top-grid'), {
  // options
  itemSelector: '.grid-item',
  columnWidth: 200
});
